# Copiez ce fichier en .env et renseignez les valeurs.
# Ne commitez jamais le fichier .env (il est dans .gitignore).

# --- Option A : Script de structure via l'API Directus ---
# URL de votre instance Directus (sans slash final)
DIRECTUS_URL= https://212.47.247.119.nip.io
# Token API : Directus > utilisateur > Token > + (si invalide, utiliser ci-dessous)
DIRECTUS_TOKEN=

# Si le token ne fonctionne pas : email et mot de passe de connexion Directus
# DIRECTUS_EMAIL=admin@exemple.com
# DIRECTUS_PASSWORD=votre_mot_de_passe

# URL du SITE inno_pui (Espace apprenants) — pour le lien envoye a l'apprenant par creer-apprenant-direct.
# Ce n'est PAS l'URL Directus (212... ou autre). Exemple GitHub Pages :
# SITE_BASE_URL=https://tyrulfr.github.io/projet_inno_pui
# Si absent, le script utilise par defaut https://tyrulfr.github.io/projet_inno_pui

# --- Connexion apprenant par identifiant + mot de passe (creer-apprenant-direct) ---
# Si vous deployez l'API (dossier api/), renseignez :
# API_BASE_URL=https://votre-api.example.com
# ADMIN_SECRET=un_secret_fort_pour_proteger_la_creation_d_apprenants
# Le script generera alors un identifiant et un mot de passe au lieu d'un lien magique.

# --- Envoi du mail de bienvenue (page admin, bouton Envoyer) ---
# Si vous deployez l'API send-welcome-email, creez une cle sur https://resend.com et renseignez :
# RESEND_API_KEY=re_xxxxx
# RESEND_FROM=OSER POUR INNOVER <votre-domaine@votredomaine.com>
# (ou utilisez onboarding@resend.dev pour les tests Resend)

# --- Option B : Script SQL (connexion directe PostgreSQL) ---
# Décommentez si vous exécutez scripts/schema.sql avec psql
# DATABASE_URL=postgresql://user:password@host:5432/database
